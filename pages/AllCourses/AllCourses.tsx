import styles from "./allcourses.module.css";

interface Props {
  items: string[];
}

export default function AllCourses({items}: Props) {
  return (
    <div className={styles.container}>

    </div>
  );
}